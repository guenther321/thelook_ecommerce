version: 2

models:
  - name: stg_inventory_items
    description: "Staging model for inventory items. Raw data cleaned and conformed."
    columns:
      - name: id
        description: "Primary key."
        tests:
          - not_null
          - unique
      # Add more columns as needed
