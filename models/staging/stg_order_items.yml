version: 2

models:
  - name: stg_order_items
    description: "Staging model for order items. Raw data cleaned and conformed."
    columns:
      - name: id
        description: "Primary key."
        tests:
          - not_null
          - unique
      # Add more columns as needed
