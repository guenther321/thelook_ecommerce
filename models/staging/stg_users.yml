version: 2

models:
  - name: stg_users
    description: "Staging model for users. Raw data cleaned and conformed."
    columns:
      - name: id
        description: "Primary key."
        tests:
          - not_null
          - unique
      # Add more columns as needed
