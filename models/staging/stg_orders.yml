version: 2

models:
  - name: stg_orders
    description: "Staging model for orders. Raw data cleaned and conformed."
    columns:
      - name: id
        description: "Primary key."
        tests:
          - not_null
          - unique
      # Add more columns as needed
